import { FilterChipsPipe } from './filter-chips.pipe';

describe('FilterChipsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChipsPipe();
    expect(pipe).toBeTruthy();
  });
});
